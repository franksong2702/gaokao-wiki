---
type: gaokao-cross-reading
subject: 数学
topic: 函数与导数
exam_weight: ★★★★★（占46.5%，约70分）
priority: 最高优先级
created: 2026-05-07
---

# 函数与导数 · Cross Reading 卡片

> **上海卷结构**：满分150分 | 填空12题54分 + 选择4题20分 + 解答5题76分  
> **函数与分析** 占整卷约 **46.5%**（约70分），是绝对的核心模块

---

## 面①：核心公式速查

### 基本初等函数

| 函数类型 | 表达式 | 定义域 | 值域 | 图像特征 |
|----------|--------|--------|------|----------|
| 一次函数 | y = kx + b | R | R | 直线，斜率k |
| 二次函数 | y = ax² + bx + c | R | 取决于a | 抛物线，对称轴 x = -b/2a |
| 指数函数 | y = aˣ (a>0, a≠1) | R | (0, +∞) | 过(0,1)，a>1递增 |
| 对数函数 | y = logₐx (a>0, a≠1) | (0, +∞) | R | 过(1,0)，a>1递增 |
| 幂函数 | y = xᵃ | 取决于a | 取决于a | 过(1,1) |

### 导数基本公式（必须背熟）

| f(x) | f'(x) |
|------|-------|
| C（常数） | 0 |
| xⁿ | nxⁿ⁻¹ |
| sin x | cos x |
| cos x | -sin x |
| eˣ | eˣ |
| aˣ | aˣ ln a |
| ln x | 1/x |
| logₐx | 1/(x ln a) |

### 导数运算法则

| 法则 | 公式 |
|------|------|
| 加减法 | (u ± v)' = u' ± v' |
| 乘法 | (uv)' = u'v + uv' |
| 除法 | (u/v)' = (u'v - uv')/v² |
| 链式法则 | [f(g(x))]' = f'(g(x)) · g'(x) |

---

## 面②：解题套路（从哪来 → 干什么 → 怎么做）

### 套路1：求单调区间

```
给 f(x) → 求 f'(x) → 解 f'(x) > 0 → 递增区间
                   → 解 f'(x) < 0 → 递减区间
```

**⚠️ 易错点**：
- 先求**定义域**，再求导！定义域限制了单调区间的范围
- f'(x) ≥ 0 中 "=" 只在孤立点成立时，仍算单调递增

### 套路2：求极值与最值

```
给 f(x) 在 [a, b] 上 → 求 f'(x) → 找 f'(x) = 0 的点 → 列表判断
                                           ↓
                        f'(x) 左+右- → 极大值
                        f'(x) 左-右+ → 极小值
                                           ↓
               比较极值 + 端点值 f(a), f(b) → 最值
```

**⚠️ 易错点**：
- 极值 ≠ 最值！最值需要**比较端点**
- f'(x) = 0 只是必要条件，还要判断左右符号是否变化

### 套路3：切线方程

```
已知切点 (x₀, f(x₀)) → 斜率 k = f'(x₀) → 切线: y - f(x₀) = f'(x₀)(x - x₀)
```

**⚠️ 易错点**：
- 注意区分"在点P处的切线"（P在曲线上）和"过点P的切线"（P可能不在曲线上）

### 套路4：含参问题（恒成立 / 有解）

```
f(x) ≥ a 恒成立 → f(x)min ≥ a
f(x) ≤ a 恒成立 → f(x)max ≤ a
f(x) ≥ a 有解 → f(x)max ≥ a
f(x) ≤ a 有解 → f(x)min ≤ a
```

**⚠️ 易错点**：
- "恒成立"用**最值**，"有解/存在"也用**最值**但方向相反
- 参数分离法是常用技巧：把含参的移到一边

---

## 面③：函数性质速查

### 奇偶性

| 类型 | 定义 | 图像特征 | 例子 |
|------|------|----------|------|
| 奇函数 | f(-x) = -f(x) | 关于原点对称 | sin x, x³, 1/x |
| 偶函数 | f(-x) = f(x) | 关于y轴对称 | cos x, x², |x| |

**判定步骤**：
1. 先看定义域是否关于原点对称（不对称则非奇非偶）
2. 计算 f(-x)，与 f(x) 比较

### 周期性

| 条件 | 周期 |
|------|------|
| f(x + T) = f(x) | T 是周期 |
| f(x + a) = -f(x) | T = 2a |
| f(x + a) = 1/f(x) | T = 2a |
| f(x + a) = -1/f(x) | T = 2a |

### 对称性

| 条件 | 对称性 |
|------|--------|
| f(a + x) = f(a - x) | 关于 x = a 对称 |
| f(x) = f(2a - x) | 关于 x = a 对称 |
| f(a + x) + f(a - x) = 2b | 关于点 (a, b) 对称 |

---

## 面④：上海卷考法统计

### 各题型中函数的位置

| 题型 | 位置 | 难度 | 常见考点 |
|------|------|------|----------|
| 填空1-6 | 基础题（4分） | ★ | 定义域、值域、奇偶性、反函数 |
| 填空7-12 | 中档题（5分） | ★★~★★★ | 函数图像变换、性质综合、零点 |
| 选择13-16 | 中档~较难（5分） | ★★★ | 函数性质综合判断 |
| 解答17-19 | 中档（14分） | ★★★ | 导数求单调区间、极值 |
| 解答20-21 | 压轴（16~18分） | ★★★★★ | 含参讨论、零点问题、不等式证明 |

### 压轴题高频考法

1. **含参函数的单调性讨论** → 分类讨论（按参数取值分段）
2. **零点个数问题** → 转化为函数图像交点
3. **不等式恒成立** → 最值法 / 参数分离
4. **极值点偏移** → 构造新函数

---

## 面⑤：最后30天复习策略

### 每天10分钟扫一遍

```
Day 1-2: 导数公式 + 运算法则 → 做3道求导练习
Day 3-4: 单调性判断 → 做3道含参讨论
Day 5-6: 极值最值 → 做3道闭区间最值
Day 7-8: 切线方程 → 做3道"过点"切线（注意区分"在点"）
Day 9-10: 函数性质综合（奇偶+周期+对称）
```

### ⚠️ 考前必查5个易错点

| # | 易错点 | 记住 |
|---|--------|------|
| 1 | 求导前不先看定义域 | **定义域优先** |
| 2 | 极值点没验证左右符号变化 | f'(x)=0 ≠ 极值点 |
| 3 | 最值忘记比较端点 | 极值 ≠ 最值 |
| 4 | "过点"切线和"在点"切线混淆 | 点不一定在曲线上 |
| 5 | 含参讨论漏掉临界情况 | 分类讨论要**完整** |

### 二级结论（可选记忆，提速用）

| 结论 | 条件 |
|------|------|
| f(x) + f(-x) = 2b | f(x) 关于 (0, b) 对称 |
| f(a+x) = f(b-x) | 对称轴 x = (a+b)/2 |
| y = f(x) 与 y = f(2a-x) 的图像 | 关于 x = a 对称 |

---

*卡片生成时间：2026-05-07 | 适用于上海高考数学 | 函数模块占比约46.5%*
